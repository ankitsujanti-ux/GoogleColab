{
  "id": "week1-day4",
  "title": "Day 4 — Transformers, Tokens & API",
  "summary": "Read in order: Step 1 → 4. Each step uses the correct terms and builds on the previous one.",
  "sections": [
    {
      "step": 1,
      "heading": "What GPT stands for",
      "remember": "**GPT** = **G**enerative **P**re-trained **T**ransformer — a model that predicts the next **token** using a **transformer** trained on large data.",
      "points": [
        "**Generative** — output is generated token-by-token (next-token prediction), not a single fixed label.",
        "**Pre-trained** — weights are learned on large corpora first; your API call uses that base model (plus any product tuning).",
        "**Transformer** — the **architecture** (how layers are wired); Step 2 explains why it matters."
      ],
      "visual": {
        "type": "three-cards",
        "cards": [
          { "label": "G", "title": "Generative", "text": "Next-token prediction" },
          { "label": "P", "title": "Pre-trained", "text": "Trained before your prompt" },
          { "label": "T", "title": "Transformer", "text": "Architecture (Step 2)" }
        ]
      }
    },
    {
      "step": 2,
      "heading": "Transformer (the T in GPT)",
      "remember": "A **transformer** uses **self-attention** on **sequences** so each token can use context from earlier tokens — that enabled scaling to modern **LLMs**.",
      "points": [
        "Comes from the 2017 paper *Attention Is All You Need* — an **architecture**, not just more data.",
        "**Self-attention** — when processing one token, the model learns which other tokens in the sequence to weight.",
        "Why it matters for you: efficient training at scale → large **parameters**, lower cost per capability vs older sequence models.",
        "Internals (layers, decoders) come later in the course; today = purpose and vocabulary only."
      ],
      "visual": {
        "type": "attention",
        "sentence": "The cat sat on the mat",
        "focus": "sat",
        "looksAt": ["cat", "on", "mat"]
      }
    },
    {
      "step": 3,
      "heading": "Tokens & tiktoken",
      "remember": "The model reads **token IDs**, not raw English — **tiktoken** encodes/decodes; **API cost** and **context window** limits are counted in tokens.",
      "points": [
        "**Tokenization** splits text into subword **tokens**; one word can be multiple tokens.",
        "Lab: `tiktoken.encoding_for_model(...)` → `encode()` (text → IDs), `decode()` (IDs → text).",
        "**Context window** — max tokens per request (input + output); longer prompts use more of the window.",
        "Billing is per token — longer conversations cost more (ties to Step 4)."
      ],
      "visual": {
        "type": "tokens",
        "text": "Hi my name is Ed",
        "pieces": ["Hi", " my", " name", " is", " Ed"]
      }
    },
    {
      "step": 4,
      "heading": "Stateless API & the messages list",
      "remember": "Each `chat.completions` call is **stateless** — you must pass the full **messages** array every time; continuity is **context**, not server memory.",
      "points": [
        "The API does not store prior turns; a new HTTP request starts with no hidden history.",
        "**messages** = `[{role, content}, ...]` — typically **system**, **user**, **assistant** roles for each turn.",
        "The model runs **next-token prediction** on the entire serialized conversation you send in that request.",
        "If an earlier turn said “I'm Ed” and you omit it from `messages`, the model cannot infer it — notebook demo: name question fails without history, succeeds with full thread.",
        "**ChatGPT**-style products resend the growing thread each message → illusion of **memory**; you pay tokens for all of it."
      ],
      "visual": {
        "type": "compare",
        "title": "Same API — different messages payload",
        "left": {
          "label": "Incomplete context",
          "bad": true,
          "lines": [
            "messages: [user: What's my name?]",
            "No prior turn with the name",
            "Response: unknown"
          ]
        },
        "right": {
          "label": "Full context",
          "bad": false,
          "lines": [
            "messages: [system, user: I'm Ed, assistant: Hi Ed!, user: What's my name?]",
            "Name present in context",
            "Response: Ed"
          ]
        }
      }
    }
  ],
  "cheatSheet": [
    "GPT — Generative (next token) · Pre-trained · Transformer architecture",
    "Transformer — self-attention on sequences; scales modern LLMs",
    "Token — subword unit; tiktoken; context window & cost",
    "API — stateless; pass full messages[] each call; context ≠ memory"
  ]
}
